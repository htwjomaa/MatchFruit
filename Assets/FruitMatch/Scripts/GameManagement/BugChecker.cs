using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BugChecker : MonoBehaviour
{
    /*
    float timer = 0.1f;
    void Update()
    {
        timer -= Time.deltaTime;
        if (timer < 0)
        {
            timer = 0.1f;
        //    bugChecking(); }
        
    }

    // private void bugChecking()
    // {
    //     foreach (IconScript icon in FindObjectsOfType<IconScript>())
    //     {
    //         if(icon.sideDotRef == null || icon.sideDotRef.Icon != icon.gameObject) icon.DestroyItself();
    //     }
    //     foreach (SideDot sideDot in FindObjectsOfType<SideDot>())
    //     {
    //         if(sideDot.Icon == null) sideDot.PrepareSideDot();
    //     }
    // }
    */
}
